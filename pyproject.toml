[project]
name = "kiddo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.2",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=4.4.6",
    "djlint>=1.35.2",
]
